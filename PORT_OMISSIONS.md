# PORT_OMISSIONS.md


<!-- ══════════════════════════════════════════════════════════════════════════
BEFORE YOU ADD AN ENTRY TO THIS FILE — READ THIS.

Every entry here is a place the parity checker STOPS comparing. That is a real cost:
a divergence you list is a divergence no gate will ever catch again. So entries must
be RARE, and each one must earn its place. Default to skepticism: assume the entry is
NOT needed and make the case that it is.

The order of preference, always:
  1. FIX THE PORT so it matches the reference (add the missing member; make the
     signature match).
  2. FIX THE EMISSION so idiom folds onto the reference shape — the enumerator/emitter
     canonicalizes your language's spelling onto the oracle's (builder → __init__,
     getters → attributes, Result<T,E> → the plain return, CamelCase → the reference
     name, options-object/kwargs → the expanded param list, RAII/dispose → close).
     MOST divergences are idiom and belong here, not in this file.
  3. FIX THE REFERENCE if the oracle itself is wrong or stale (a Python-only symbol
     that leaked into the contract, a param the reference added and the oracle never
     re-enumerated). Fix Python / the oracle, then re-drift — do not paper over a
     broken reference with a per-port entry.
  4. Only when 1–3 genuinely cannot apply does an entry here become justified.

An entry is JUSTIFIED ONLY IF it is irreducible after correct emission — i.e. the
divergence survives because the two languages genuinely cannot express the same thing,
not because the emitter hasn't folded the idiom yet. If emission COULD fold it, the
entry is a bug in this file; go fix the emitter.

Each entry MUST state WHY, concretely, in one of these forms:
  • ADDITION — this symbol exists in the port but not the reference. Answer: is it
    genuine port-only surface with NO reference twin (say what it is and why the
    reference has no equivalent), or is it IDIOM the emitter should have folded (then
    it does not belong here — fold it)? A convenience/alias/back-compat wrapper is NOT
    a justification.
  • OMISSION — this reference symbol has no port member. Answer: WHY can it not exist
    here — what specific language feature is absent (e.g. no async-context-manager
    protocol, no __init__ method protocol)? "impossible:" means the construct cannot
    be expressed at all; if it merely LOOKS different, that's idiom → fold it, don't
    omit it. Cite a precedent when one exists (e.g. RelayClient omits the same dunder).
  • SIGNATURE — the symbol matches by name but its parameters differ. Answer: is the
    difference a foldable idiom collapse (options-object, leading context/self,
    builder) — then EXPAND it in the signature emitter so names+count match, don't list
    it — or a genuine reference-only parameter with no cross-language analogue?

If you cannot write a crisp, specific WHY that survives the "could emission fold this?"
test, the entry is not ready. Prove it's needed before you add it.
═══════════════════════════════════════════════════════════════════════════════ -->

Python-reference symbols the Perl port does not implement, each with an
explicit impossible:/approved: rationale.
One line per symbol: `<fully.qualified.symbol>: <one-sentence rationale>`.
Checked by `scripts/diff_port_surface.py` against `python_surface.json`.

See also: PORT_ADDITIONS.md for Perl-only extensions.


## Reference-symbol omissions (impossible:/approved: only)

Each line: `<symbol>: impossible: <why the OO-idiom cousin TS/PHP also can't>` or
`<symbol>: approved: <human sign-off>`. Everything else is a bug — the gate
(`diff_port_surface.py`) rejects any other prefix. Item H/I pass, 2026-07.

signalwire.core.agent.tools.decorator.ToolDecorator: impossible: Python @tool decorator-protocol API; Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically)
signalwire.core.agent.tools.decorator.ToolDecorator.create_class_decorator: impossible: Python @tool decorator-protocol API; Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically)
signalwire.core.agent.tools.decorator.ToolDecorator.create_instance_decorator: impossible: Python @tool decorator-protocol API; Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically)
signalwire.core.agent.tools.registry.ToolRegistry.register_class_decorated_tools: impossible: Python @tool decorator-protocol API; Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically)
signalwire.core.mixins.mcp_server_mixin.MCPServerMixin: impossible: method-less Python mixin class hosting add_mcp_server/enable_mcp_server; per the user ruling those two methods ARE ported (folded onto AIConfigMixin/AgentBase, present), but the standalone mixin CLASS itself has no Perl home — Perl composes AgentBase via roles, not a named MCPServerMixin class (TS/PHP fold identically)
signalwire.core.mixins.tool_mixin.ToolMixin.tool: impossible: Python @tool decorator-protocol API; Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically). Raw ToolMixin.tool key for the SIGNATURE diff; folded agentbase-family.tool twin excused for the SURFACE diff.
agentbase-family.tool: impossible: Python @tool decorator-protocol API (folds to agentbase-family via the mixin-flatten fold); Perl/Moo has no method-decorator feature — tools register via define_tool directly (TS+PHP omit identically)
signalwire.agent_server.AgentServer.logger: impossible: Python holds a logging.Logger as a composition attr (logger -> class:logging_config.get_logger); Perl has no logger-attribute idiom exposing a Logger class — it logs via warn/module-level helpers, so there is no self-only accessor returning a Logger (TS/PHP getter-idiom ports surface theirs; Perl's Moo class does not).
signalwire.core.skill_base.SkillBase.logger: impossible: see AgentServer.logger — Perl SkillBase exposes no logger composition accessor (no Logger-class-holding attr in the Perl logging idiom).
signalwire.core.skill_manager.SkillManager.logger: impossible: see AgentServer.logger — Perl SkillManager exposes no logger composition accessor.
signalwire.skills.registry.SkillRegistry.logger: impossible: see AgentServer.logger — Perl SkillRegistry exposes no logger composition accessor.
signalwire.web.web_service.WebService.security: impossible: Python's WebService holds a SecurityConfig composition attr (security -> class:security_config.SecurityConfig); Perl's WebService applies security inline via a private _security_headers() sub and exposes no self-only accessor returning a SecurityConfig class.
signalwire.core.security.webhook_middleware.make_webhook_validation_dependency: impossible: framework-bound factory returning a FastAPI dependency; Perl ships the equivalent as the SignalWire::Security::WebhookMiddleware Plack middleware (a PORT_ADDITION) — the FastAPI-dependency FORM has no Plack analog (TS/PHP ship native middleware likewise)
signalwire.livewire.Agent: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.llm_node: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.on_enter: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.on_exit: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.on_user_turn_completed: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.session: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.stt_node: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.tts_node: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.update_instructions: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Agent.update_tools: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentHandoff: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentHandoff.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentServer: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentServer.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentServer.rtc_session: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.generate_reply: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.history: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.interrupt: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.say: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.start: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.update_agent: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.AgentSession.userdata: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.ChatContext: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.ChatContext.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.ChatContext.append: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.function_tool: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceLLM: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceLLM.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceSTT: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceSTT.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceTTS: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.InferenceTTS.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobContext: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobContext.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobContext.connect: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobContext.wait_for_participant: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobProcess: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.JobProcess.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.CartesiaTTS: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.CartesiaTTS.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.DeepgramSTT: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.DeepgramSTT.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.ElevenLabsTTS: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.ElevenLabsTTS.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.OpenAILLM: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.OpenAILLM.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.SileroVAD: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.SileroVAD.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.plugins.SileroVAD.load: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.Room: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.run_app: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.RunContext: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.RunContext.__init__: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.RunContext.userdata: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.StopResponse: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.livewire.ToolError: approved: livewire is LiveKit-agents-compat; LiveKit ships no Perl agents SDK (only Python + Node/TS), so it is not ported to Perl (§I.1/L21)
signalwire.relay.client.RelayClient.__aenter__: impossible: Python async-context-manager protocol dunder; Perl uses explicit connect+disconnect — no __aenter__/__aexit__ equivalent (TS/PHP omit identically)
signalwire.relay.client.RelayClient.__aexit__: impossible: Python async-context-manager protocol dunder; Perl uses explicit connect+disconnect — no __aenter__/__aexit__ equivalent (TS/PHP omit identically)
signalwire.ai_chat.client.AIChatClient.__aenter__: impossible: Python async-context-manager protocol dunder; Perl has no context-manager language protocol — the AIChat client is used directly (construct-and-call), no with-block enter (TS/PHP omit identically)
signalwire.ai_chat.client.AIChatClient.__aexit__: impossible: Python async-context-manager protocol dunder; Perl has no context-manager language protocol — no with-block exit (TS/PHP omit identically)
signalwire.relay.client.RelayClient.__del__: impossible: Python finalizer dunder; Perl has no deterministic __del__ finalizer protocol (TS/PHP omit identically)
signalwire.relay.message.Message.__repr__: impossible: Python object-repr dunder; Perl provides the equivalent via a to_string method where user-facing, but the __repr__ FORM has no Perl analog on this data carrier (TS/PHP omit identically)
signalwire.rest._base.FabricResourcePUT: impossible: the Perl generated FabricResource base collapses the reference's CrudWithAddresses -> FabricResource(PATCH) / FabricResourcePUT(PUT) marker split into a single base carrying list_addresses; the PUT-vs-PATCH verb is baked per-resource via _update_method in each generated class's BUILDARGS, so there is no separate method-less PUT marker class to emit (a generated-layout limit the TS/PHP generators hit identically)



# ---------------------------------------------------------------------------
# NOTE — AIParams fields dropped via porting-sdk/rest-apis/x-sdk-overlay.yaml
# ---------------------------------------------------------------------------
# The following 5 fields of the spec schema AIParams (schema.json $defs/AIParams
# and components/schemas/AIParams in the calling + fabric REST specs) are HIDDEN
# from the SDK surface by the single authoritative overlay x-sdk-overlay.yaml —
# still accepted on the wire, dropped from the generated Perl AIParams data
# packages (REST Types/Calling, Types/Fabric, and SWML/Generated):
#   - audible_debug
#   - audible_latency
#   - verbose_logs
#   - enable_accounting
#   - cache_mode
# Reason: hidden via x-sdk-overlay.yaml: server-internal AI param, dropped from
# SDK surface per overlay policy (approved).
#
# These are NOT ledgered as diff_port_surface.py symbol lines: the surface oracle
# records AIParams as a method-less TYPE (class name only) and does not track its
# field members, so field-level hides never surface as SURFACE-DIFF omissions
# (the gate passes clean). This note is documentation only; the checker skips
# every '#'-prefixed line. languages_enabled is NOT dropped — it is emitted and
# marked '# deprecated:' per the overlay's deprecated list, so it is not recorded
# here either.

## B1 composition-attr / generated-model field-accessor omissions (fold branch)
# porting-sdk fix/agentbase-mixin-flatten-fold enriched the surface oracle with class-ref
# composition attributes + generated-model per-field accessors. Perl models generated payload
# classes as method-less DTOs and does not surface Moo `has` accessors as API — same idiom
# disposition ruby/ts/php take (wire JSON identical). See ALLOWLIST_DISCIPLINE.md §4c/§6-B1.

agentbase-family.skill_manager: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs). Folded key for the SURFACE diff.
signalwire.agent_server.AgentServer.agents: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.pom_builder.PomBuilder.pom: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.post_prompt_generated.PostPrompt.call_log: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPrompt.post_prompt_data: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPrompt.raw_call_log: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPrompt.swaig_log: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPrompt.times: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPromptAssistantEntry.stamps_us: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPromptSwaigLogEntry.post_data: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPromptUserEntry.entity: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPromptUserEntry.eot: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.post_prompt_generated.PostPromptUserEntry.timing: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.skill_manager.SkillManager.loaded_skills: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.swaig_request_generated.SwaigRequest.argument: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_service.SWMLService.schema_utils: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.swml_service.SWMLService.security: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.swml_service.SWMLService.verb_registry: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.core.swml_verbs_generated.AI: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AI.ai: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.SWAIG: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.hints: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.languages: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.params: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.post_prompt: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.prompt: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIObject.pronounce: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIParams: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIParams.convo: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPostPromptPom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPostPromptPom.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPromptPom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPromptPom.contexts: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPromptPom.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPromptText: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AIPromptText.contexts: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AllOfProperty: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AllOfProperty.allOf: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrock: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrock.amazon_bedrock: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrockObject: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrockObject.SWAIG: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrockObject.params: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrockObject.post_prompt: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AmazonBedrockObject.prompt: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AnyOfProperty: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.AnyOfProperty.anyOf: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ArrayProperty: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ArrayProperty.items: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.BedrockSWAIG: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.BedrockSWAIG.defaults: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.BedrockSWAIG.functions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.BedrockSWAIG.includes: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.BedrockSWAIG.native_functions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Cond: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Cond.cond: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.CondReg: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.CondReg.then: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectConfig.call_state_events: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectConfig.headers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectConfig.parallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectConfig.serial: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectConfig.serial_parallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceParallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceParallel.call_state_events: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceParallel.headers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceParallel.parallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerial: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerial.call_state_events: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerial.headers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerial.serial: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerialParallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerialParallel.call_state_events: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerialParallel.headers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSerialParallel.serial_parallel: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSingle: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSingle.call_state_events: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectDeviceSingle.headers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectSwitch: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConnectSwitch.default: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextPOMSteps: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextPOMSteps.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Contexts: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Contexts.default: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsPOMObject: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsPOMObject.enter_fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsPOMObject.exit_fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsPOMObject.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsPOMObject.steps: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsTextObject: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsTextObject.enter_fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsTextObject.exit_fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ContextsTextObject.steps: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConversationMessage: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ConversationMessage.role: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.DataMap: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.DataMap.expressions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.DataMap.output: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.DataMap.webhooks: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.EnterQueue: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.EnterQueue.enter_queue: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ExecuteConfig.on_return: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ExecuteSwitch: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.ExecuteSwitch.default: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Expression: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Expression.output: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.HangUpHookSWAIGFunction: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.HangUpHookSWAIGFunction.argument: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.HangUpHookSWAIGFunction.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.HangUpHookSWAIGFunction.fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.HangUpHookSWAIGFunction.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.JoinConference: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.JoinConference.join_conference: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LanguagesWithFillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LanguagesWithFillers.params: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LanguagesWithSoloFillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LanguagesWithSoloFillers.params: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LiveTranscribeConfig.action: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.LiveTranslateConfig.action: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.OmitPropertiesBedrockPostPromptPomOmittedPromptProps.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.OmitPropertiesBedrockPromptPomOmittedPromptProps.pom: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.OneOfProperty: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.OneOfProperty.oneOf: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Output: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Output.action: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PayConfig.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PayConfig.prompts: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PayPrompts: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PayPrompts.actions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesHangUpHookSWAIGFunctionPickedSWAIGFunctionProps.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesHangUpHookSWAIGFunctionPickedSWAIGFunctionProps.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesStartUpHookSWAIGFunctionPickedSWAIGFunctionProps.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesStartUpHookSWAIGFunctionPickedSWAIGFunctionProps.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesSummarizeConversationSWAIGFunctionPickedSWAIGFunctionProps.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesSummarizeConversationSWAIGFunctionPickedSWAIGFunctionProps.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesUserSWAIGFunctionPickedSWAIGFunctionProps.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PickPropertiesUserSWAIGFunctionPickedSWAIGFunctionProps.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PomSectionBodyContent: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PomSectionBodyContent.subsections: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PomSectionBulletsContent: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.PomSectionBulletsContent.subsections: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG.defaults: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG.functions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG.includes: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG.internal_fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIG.native_functions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.adjust_response_latency: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.change_context: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.check_time: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.get_ideal_strategy: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.get_visual_input: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.hangup: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.next_step: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.wait_for_user: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SWAIGInternalFiller.wait_seconds: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Section: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Section.main: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StartUpHookSWAIGFunction: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StartUpHookSWAIGFunction.argument: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StartUpHookSWAIGFunction.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StartUpHookSWAIGFunction.fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StartUpHookSWAIGFunction.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StringProperty: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.StringProperty.format: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SummarizeConversationSWAIGFunction: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SummarizeConversationSWAIGFunction.argument: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SummarizeConversationSWAIGFunction.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SummarizeConversationSWAIGFunction.fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SummarizeConversationSWAIGFunction.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.SwitchConfig.default: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.UserSWAIGFunction: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.UserSWAIGFunction.argument: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.UserSWAIGFunction.data_map: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.UserSWAIGFunction.fillers: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.UserSWAIGFunction.parameters: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Webhook: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Webhook.expressions: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.core.swml_verbs_generated.Webhook.output: impossible: Python exposes this as a generated-model (Pydantic) per-field accessor member (porting-sdk B1 composition-attr enrichment); Perl records its generated payload .pm classes as method-less pure-data DTOs (documented in enumerate_surface.pl) with no per-field accessor surface — the wire JSON is identical (ruby/ts/php omit the same generated-model fields).
signalwire.pom.pom.PromptObjectModel.sections: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.pom.pom.Section.subsections: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.relay.call.Action.result: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest._request_options.RequestOptions.abort_signal: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.DatasphereNamespace.documents: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.addresses: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.ai_agents: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.call_flows: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.conference_rooms: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.cxml_applications: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.cxml_scripts: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.cxml_webhooks: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.freeswitch_connectors: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.relay_applications: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.resources: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.sip_endpoints: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.sip_gateways: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.subscribers: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.swml_scripts: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.swml_webhooks: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.FabricNamespace.tokens: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.LogsNamespace.conferences: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.LogsNamespace.fax: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.LogsNamespace.messages: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.LogsNamespace.voice: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.ProjectNamespace.tokens: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.RegistryNamespace.brands: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.RegistryNamespace.campaigns: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.RegistryNamespace.numbers: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.RegistryNamespace.orders: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.conference_tokens: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.conferences: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.room_recordings: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.room_sessions: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.room_tokens: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.rooms: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).
signalwire.rest.namespaces._client_tree_generated.VideoNamespace.streams: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs).

# Raw-key twins for the SIGNATURE diff (dual-key convention)
signalwire.core.agent_base.AgentBase.skill_manager: impossible: Python exposes this as a composition-attribute accessor returning an SDK class (porting-sdk B1 enrichment); Perl's surface enumerator is method(sub)-based and does not surface Moo `has` composition accessors as public API — the held object is reachable but not an enumerated surface member (ruby/ts/php omit the same composition attrs). Raw key for the SIGNATURE diff (folded agentbase-family twin above).
